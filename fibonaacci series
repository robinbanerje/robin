#include <stdio.h>

void fibo(int num)
{
 int a=0,b=1,add=0;
  for(int i=0;i<num;i++)
    {
    printf("%d ",a);
    add=a+b;
    a=b;
    b=add;
    }  
}

int main()
{   int n;
    printf("\nenter the number of fibonacci terms u want to find:\n");
    scanf("%d",&n);
    fibo(n);
    return 0;
}
